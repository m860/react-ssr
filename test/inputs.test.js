import React from "react";
import {mount} from "enzyme";
import TextInput from '../src/components/inputs/TextInput'

describe(`test <TextInput/>`, () => {
	test(`label is required`, () => {
	});
	test(`allow to set props.defaultValue`, () => {
	})
	test(`allow to set props.value`, () => {
	})
	test(`can pass correct value when change event is fired`, () => {
	})
	test(`can toggle the error message when user input invalid or valid value`, () => {
	})
})